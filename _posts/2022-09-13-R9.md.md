# 배열

- 생성함수
    - array (data = NA, dim = length(data), dimnames = NULL)
        - data : data를 저장한 벡터
        - dim=length(data) : 배열의 차원 / 지정하지 않으면 1차원
        - dimnames : 차원의 이름

![Untitled](Untitled%2010.png)